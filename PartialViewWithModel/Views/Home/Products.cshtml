@*@model IEnumerable<PartialViewWithModel.Models.Product>

    <h1>Products</h1>

    <div class="container">
        <div class="row">
            @foreach (var product in Model)
            {
               <div class="col-md-4">
                   <partial name ="_partialView"/>
               </div>
            }
        </div>
    </div>*@
@model IEnumerable<PartialViewWithModel.Models.Product>

    <h1>Products</h1>

    <div class="container">
        <div class="row">
            @foreach (var product in Model)
            {
                <div class="col-md-4">
                    <partial name="_partialView" model="product" />  <!-- ✅ Pass SINGLE product -->
                </div>
            }
        </div>
    </div>
